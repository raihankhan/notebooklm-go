name: ci

on:
  push:
    branches: [master, ticket/**]
  pull_request:
    branches: [master]

jobs:
  build:
    name: build (${{ matrix.os }}/${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        arch: [amd64, arm64]
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          check-latest: true

      - name: setup golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          # Pin to v1.66.x: v1.64/1.65 ship with Go 1.24 toolchain and refuse to
          # parse a go.mod declaring `go 1.25`. Re-bump only after verifying
          # release notes cover the target Go version.
          version: v1.66.2

      - name: go env
        run: go env GOOS GOARCH

      - name: build
        run: make build

  test:
    name: test (${{ matrix.os }}/${{ matrix.arch }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        arch: [amd64, arm64]
        exclude:
          - os: macos-latest
            arch: amd64
    steps:
      - name: checkout
        uses: actions/checkout@v4

      - name: setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          check-latest: true

      - name: setup golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          # Pin to v1.66.x: v1.64/1.65 ship with Go 1.24 toolchain and refuse to
          # parse a go.mod declaring `go 1.25`. Re-bump only after verifying
          # release notes cover the target Go version.
          version: v1.66.2

      - name: check
        run: make check
